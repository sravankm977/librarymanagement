import { HoverZoomDirective } from './hover-zoom-directive';

describe('HoverZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverZoomDirective();
    expect(directive).toBeTruthy();
  });
});
