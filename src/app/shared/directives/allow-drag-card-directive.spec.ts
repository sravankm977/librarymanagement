import { AllowDragCardDirective } from './allow-drag-card-directive';

describe('AllowDragCardDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowDragCardDirective();
    expect(directive).toBeTruthy();
  });
});
