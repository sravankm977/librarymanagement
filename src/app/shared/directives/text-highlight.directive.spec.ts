import { TextHighlight } from './text-highlight.directive';

describe('TextHighlight', () => {
  it('should create an instance', () => {
    // const directive = new TextHighlight(el);
    // expect(directive).toBeTruthy();
  });
});
