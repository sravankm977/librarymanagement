import { FormInputFontDirective } from './form-input-font-directive';

describe('FormInputFontDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputFontDirective();
    expect(directive).toBeTruthy();
  });
});
