import { DisableIfEmptyDirective } from './disable-if-empty-directive';

describe('DisableIfEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
