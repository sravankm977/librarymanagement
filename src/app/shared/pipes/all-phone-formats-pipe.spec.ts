import { AllPhoneFormatsPipe } from './all-phone-formats-pipe';

describe('AllPhoneFormatsPipe', () => {
  it('create an instance', () => {
    const pipe = new AllPhoneFormatsPipe();
    expect(pipe).toBeTruthy();
  });
});
